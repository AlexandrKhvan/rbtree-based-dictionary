# Словарь на основе Красно-Черного Дерева (Red-Black Tree Dictionary)

Этот проект представляет собой реализацию эффективного словаря на языке C. В основе хранения данных лежит сбалансированное Красно-Черное Дерево (Red-Black Tree), что обеспечивает логарифмическую сложность $O(\log N)$ для операций поиска, вставки и удаления.

## 📋 Функциональность

### Управление словами:

- Добавление новых слов.

- Быстрый поиск.

- Безопасное удаление слов с автоматической перебалансировкой дерева.

- Переименование слов.

### Система синонимов:

- Связывание слов как синонимов.

- Хранение синонимов в виде графа ссылок (указателей) внутри структуры узла, что экономит память и ускоряет доступ.

- Автоматическая очистка "битых" ссылок при удалении слова.

### Поиск по префиксу

Нахождение всех слов, начинающихся с заданной подстроки (автодополнение).

### Персистентность данных

Сохранение словаря в текстовый файл dictionary.txt. Интеллектуальная загрузка в два прохода (сначала построение дерева, затем восстановление связей синонимов).

## 📂 Структура Проекта

Проект разделен на модули для удобства поддержки и расширения:

.
├── include/           # Заголовочные файлы

│   ├── rbtree.h       # Структуры и прототипы ядра дерева

│   └── dict.h         # Прототипы логики словаря

├── src/               # Исходный код реализации

│   ├── rbtree.c       # Реализация RBT (повороты, балансировка)

│   └── dict.c         # Логика синонимов, файлов, поиска

├── main.c             # Точка входа и консольное меню

├── Makefile           # Сценарий сборки

└── README.md          # Документация


## 🛠 Сборка и Запуск

Для сборки проекта требуется компилятор gcc и утилита make.

Сборка проекта:
Выполните команду в корневой папке проекта:

```
make
```

После успешной компиляции появится исполняемый файл dictionary_app:

```
./dictionary_app
```

(На Windows может потребоваться запуск dictionary_app.exe)

Очистка (удаление объектных файлов):

```
make clean
```

## 💻 Использование

При запуске программы пользователю предлагается интерактивное меню:

- Добавить слово: Вводит новое слово в дерево. Дубликаты не допускаются.

- Найти слово: Выводит информацию о слове и список его синонимов.

- Удалить слово: Удаляет узел и восстанавливает свойства красно-черного дерева. Также удаляет это слово из списков синонимов других слов.

- Добавить синоним: Создает двустороннюю связь между двумя существующими словами.

- Найти по префиксу: Выводит список слов, начинающихся с введенной строки (например, "pro" -> "program", "process", "project").

- Переименовать слово: Изменяет слово, сохраняя все его связи с синонимами.

- Показать дерево: Выводит структуру дерева в консоль с отступами и указанием цвета узла (R/B) для отладки.

- Сохранить/Загрузить: Работа с файлом dictionary.txt.

## ⚙️ Технические детали

### Структура Узла

Каждый узел дерева (Node) содержит:

- word: Строка (ключ).

- color: Цвет узла (RED/BLACK).

- left, right, parent: Указатели навигации по дереву.

- synonyms[]: Массив указателей на другие узлы дерева.

### Алгоритм Балансировки

Используется классический алгоритм вставки и удаления для красно-черных деревьев:

- Вставка: Новые узлы всегда красные. Конфликты ("Красный отец - Красный сын") решаются перекраской и поворотами (Left/Right Rotate).

- Удаление: Если удаляется черный узел, дерево теряет "черную высоту". Алгоритм deleteFixup восстанавливает баланс, перемещая "двойную черноту" вверх по дереву или выполняя повороты.

### Работа с памятью

При удалении узла происходит полный обход дерева (или списков синонимов), чтобы удалить указатели на уничтожаемый узел у других слов (cleanAllReferencesTo). Это предотвращает появление висячих ссылок (dangling pointers).



Пользуйтесь
